# Uncomment the next line to define a global platform for your project
platform :ios, '8.0'

target 'KosmosBank' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for KosmosBank
  pod 'RxSwift',    '~> 3.0'
  pod 'ObjectMapper', '~> 2.2'
  pod "EasyDi"
  
end
